# Environment Variables for Docker Development
# These are test/development credentials only - NOT for production

# Database Configuration
DB_HOST=database
DB_PORT=5432
DB_NAME=saas_db
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration
JWT_SECRET=multi_tenant_saas_jwt_secret_key_for_development_only_change_in_production_32chars
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS) - Use service name in Docker
FRONTEND_URL=http://frontend:3000

# Logging
LOG_LEVEL=info
